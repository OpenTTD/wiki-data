[[Translation:en/Archive/Manual/Settings/Unicode]]
{{ru/Перевод|en/Archive/Manual/Settings/Unicode|1}}


[[File:en/Archive/Manual/Settings/Arialunicode.png|thumb|right|250px|Снимок экрана в OpenTTD с использованием шрифта Arial]]
Начиная с [[en/Archive/OpenTTD/OpenTTD 0.5.0| версии 0.5.0]], в OpenTTD ввели [https://www.wikipedia.org/wiki/ru:Юникод Юникод (Unicode)]. Это позволило поддержать языки, которые используют наборы не-ASCII символов, такие как китайский или русский, но также это позволило отображать все языки, которые содержат символы не из набора ASCII

Кроме того, можно использовать разные шрифты в OpenTTD использовании этой функции с помощью трех строк в Openttd.cfg:

<pre>small_font =
medium_font =
large_font =</pre>

Small fonts are used beneath vehicles in the vehicle lists, as the date on newspapers, and other places.
Medium fonts is the majority of GUI text, town and station names, and window headers.
Large Fonts are used as newspaper headings and on the endgame Score list.

Fill these out with fonts installed on your system and they will be displayed in the game.

<pre>small_size =
medium_size =
large_size =</pre>

There is also possibility to set font sizes (in px) for given font types.
Be aware that most fonts make a poor small font, while other fonts may not make good medium or large sizes.

Because of poor small fonts, one should select bitmap font, because that is by its very nature done to be displayed small. Users of GNU/Linux systems may choose from variety range of bitmap fonts allready installed with X server, such as 5x7.pcf.gz. These fonts are small and well rendered in terms of openttd. In Debian GNU/Linux and alike distributions, it is located in <nowiki>/usr/share/fonts/X11/misc/</nowiki>.

These fonts are located in debian package [http://packages.debian.org/etch/xfonts-base xfonts-base], and after unpacking .deb by apropriate packaging software, you can acces these fonts. However, it is not sure if these fonts are possible to work on Windows boxes.

Since r10166 it is possible to use anti-aliased fonts using the 32bpp blitter. Anti-aliasing is configurable per font with the following settings:

<pre>small_aa =
medium_aa =
large_aa =</pre>