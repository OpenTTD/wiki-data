<pre> 
//This defines the points of the grid
/*struct {
     uint8 altitude:5;
     uint8 cliff_height_of_northern_tile:3;  
} grid[MAPSIZEX+1][MAPSIZEY+1],
*/

struct {
     uint32 LandscapeInfo:12;
     uint32 BuildInfo:20;
     uint8  altitude:5;
     uint8  cliff_height:3;
     uint8  generic_counter;
     uint8  generic_status;
     uint8  owner;
} MapInfo;

MapInfo _map[MAPSIZEX][MAPSIZEY];
MapInfo _more[1000];

struct {
     uint16 ref_count;
     uint8  type;
     union {
          struct {
               uint8 hedge_SW:3;
               uint8 hedge_SE:3;
          } barren;
          struct {
               uint8 some_water_info;
          } water;
          struct {
               uint8 type:4;
               uint8 dir:2;
               uint8 section:1; //End part or middle part, do we need this?
          } bridge;
          struct {
               /* ask blathijs for details */
               /* this takes the tunnels */
          } support;

     }
} LandscapeInfo;

LandscapeInfo LI[2^12]

struct {
      uint8 ref_count;
      uint8 type;
      union {
           struct {} industry;
           struct {} station;
           struct {} rail;
           struct {} road;
           struct {} town;
           struct {} trees;
           struct {} field;
           struct {} unmovable;
      }
} BuildInfo;

BuildInfo BI[2^20]
</pre>