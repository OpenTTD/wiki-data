<includeonly>|</includeonly><noinclude>
Questo è un template ausiliario che consente di codificare "|" all'interno dei parametri della funzione template e parser. Per ottenere il simbolo nell'output finale questo modello non è necessario, meglio utilizzare "&#124;" che è reso lo stesso. Utilizzare questo modello nei valori dei parametri dei template e nelle funzioni del parser di cui il risultato viene utilizzato nella sintassi della tabella, poiché ciò richiede il carattere pipe "reale".

Questo template non può essere utilizzato per inserire il carattere come testo in una tabella wiki; in tal caso, ancora una volta, è possibile utilizzare <nowiki>"&#124;"</nowiki>.

Si noti che il codice di sintassi della tabella "!" (punto esclamativo) può essere utilizzato direttamente nei valori dei parametri, quindi per questo non è necessario un modello come questo.
</noinclude>

---------------------------

<includeonly>|</includeonly><noinclude>
Questo è un modello ausiliario che consente di codificare "|" all'interno dei parametri della funzione modello e parser. Per ottenere il simbolo nell'output finale questo modello non è necessario, meglio utilizzare "&#124;" che è reso lo stesso. Utilizzare questo modello nei valori dei parametri dei modelli e nelle funzioni del parser di cui il risultato viene utilizzato nella sintassi della tabella, poiché ciò richiede il carattere pipe "reale".

Questo modello non può essere utilizzato per inserire il carattere come testo in una tabella wiki; in tal caso, ancora una volta, è possibile utilizzare <nowiki>"&#124;"</nowiki>.

Si noti che il codice di sintassi della tabella "!" (punto esclamativo) può essere utilizzato direttamente nei valori dei parametri, quindi per questo non è necessario un template come questo.
</noinclude>