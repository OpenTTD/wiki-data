<span style="font: {{{2}}} 0.85em sans-serif; padding: 1px 3px; background-color: whitesmoke; border: solid darkgray; border-width: 1px 1px 2px; border-radius: 3px;">{{{1}}}</span><noinclude><br />Meow, prototype keypress template.<br />'''Parameters'''
# is the key name, case selection currently at the discression of the user
# is optionally the font-style and font-variant, in that order.
#* styles include normal, italic, oblique and inherit (default)
#* variants include include normal, small-caps inherit (default, but does not work with all browsers when specified)
Note: this template my be altered to enforce a specific case and variant.</noinclude><noinclude>[[Category:en/Templates|{{PAGENAME}}]]</noinclude>